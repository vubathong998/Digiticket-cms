@using Services.Models.Responses.Content;
@model TemplateComponentTypeResponse

@{
    var isReadOnly = Model.Id == 0;
}
@using (Html.BeginForm(isReadOnly ? "TemplateComponentTypeAdd" : "TemplateComponentTypeEdit", "LandingPage", FormMethod.Post, new
{
    enctype = "multipart/form-data",
    @class = "kt-form ajax-form",
    @id = "mediaUpload",
    spellcheck = false,
    showLoading = true
}))
{
    if (!isReadOnly)
    {
        <input name="id" value="@Model.Id" type="hidden" />
    }
    <div class="modal-header">
        <h1 class="modal-title">
            @if (isReadOnly)
            {
                <span>Thêm mới Template component type</span>
            }
            else
            {
                <span>Sửa Template component type <span class="text-success">@Model.Name</span></span>
            }
        </h1>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
    <div class="modal-body">
        <div class="">
            <label for="name">Tên</label><label class="text-red error ml-5" for="name"></label>
            <input class="form-control" name="name" value="@(isReadOnly ? "" : Model.Name)" />
        </div>
    </div>
    <div class="modal-footer">
        <button type="submit" class="btn-upload-media btn btn-primary">Hoàn tất</button>
    </div>
}
